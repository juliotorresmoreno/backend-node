

const app = require('../build/app').default;

test("entity", async function () {
    /*var user = UsuarioModel();
    var errors = await validate(user);
    assert(errors.length == 0, "Las restricciones del modelo de usuarios no funcionan")*/
});